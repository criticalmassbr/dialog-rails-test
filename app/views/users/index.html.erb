<h1>Hello, dialog!</h1>
<h2>Users list</h2>
<ul>
    <% @users.each do |user| %>
        <li>
            <%= link_to user.name, user %>
        </li>
    <% end %>
</ul>
<%= will_paginate @users %>
<%= link_to "New User", new_user_path %>